# 2.5.1 — Bilingual README artwork

Date: 2026-09-11. Baseline: `v2.5.0`. This documentation patch replaces five image pairs and adds the six-page overview between the home and map panels.

## Changes

- Detailed ChatGPT-style fictional planning conversation, with the actual Codex skill entry distinguished in the caption.
- Matching Chinese/English portrait-phone illustrations for the home, map, tools/tickets and daily food/hotel arrangements.
- One new pair presenting all six primary pages together.
- Shared editable image sources, complete Chinese translation mapping, AI scenic-asset provenance and reproducible rendering checks.
- Full bilingual READMEs, version/install links and image descriptions updated together.

The existing application framework, static starter, audit engine and personal reference guide are unchanged. These illustrations demonstrate intended workflows; they are not live application captures or new backend acceptance evidence. Historical release records and prior artwork sources remain available.

## Verification

- 21 Python and 13 Node regression tests passed.
- Host skill metadata validator passed.
- Twelve PNG files rendered in isolated Chrome with outbound HTTP assets blocked. Each language contains twelve phone views and 72 drawn navigation icons.
- All Chinese text mappings are complete; no broken scenic assets or page-canvas overflow. Minimum content clearance above phone navigation: 12 CSS pixels.
- All six Chinese compositions visually reviewed; the English compositions preserve the approved preview, with only the footer changed from preview to permanent illustration labeling.
- Documentation version/local-link/dimension checks passed; the combined README matches the two complete language editions.
- Public content contains fictional examples and no personal reference-site data or copied production images.

See [image provenance and reproduction](../SCREENSHOTS.md) for the renderer, dimensions, font assumptions, scenic generation prompt and layout-check record.

```sh
python3 -m unittest discover -s tests -p 'test_*.py'
node --test tests/*.test.cjs
sh scripts/render_readme_screenshots.sh
python3 scripts/check_docs.py
```

## Publication contract

Publish the reviewed commit and annotated `v2.5.1` tag to the existing public repository. Export that exact tag as `travel-guide-pwa-builder-2.5.1.zip` with a `travel-guide-pwa-builder/` directory prefix, publish it with `SHA256SUMS`, then download both assets and compare all archived file bytes with the tag. Before synchronizing the installed skill, verify its prior snapshot and back it up; install only the verified release files and Git file modes.

The [GitHub Release](https://github.com/pengguin/travel-guide-pwa-builder/releases/tag/v2.5.1) provides the publication receipt. The private reference Site is not republished by this release.

## 中文摘要

本版完成六组中英 README 配图，新增六个一级页面总览，并细化手机导航、票面选框与食宿安排。发布内容包含可编辑图源、中文译文、生图来源和验证记录。34 项回归测试及元数据、图像和文档检查通过；图片属于虚构示意，不代表新增功能或真机验收。
